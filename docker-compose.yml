services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: gasflare
      POSTGRES_PASSWORD: gasflare
      POSTGRES_DB: gasflare
    ports:
      - "${POSTGRES_HOST_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
