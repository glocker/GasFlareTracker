DATABASE_URL=postgresql://gasflare:gasflare@localhost:5432/gasflare
# only needed if port 5432 is already taken locally (docker-compose reads this too)
# POSTGRES_HOST_PORT=5433
FIRMS_MAP_KEY=
CORS_ORIGINS=http://localhost:5173
